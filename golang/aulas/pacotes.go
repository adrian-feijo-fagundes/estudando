package aulas

import (
	"fmt"
	str "strings"
)

func Pacotes() {
	fmt.Println("Hello, world!")

	// sem alterar o nome do pacote,
	// usaríamos chamando strings.Func()
	// strings.Slipt()
	fmt.Println(str.Split("Adrian", ""))
}
